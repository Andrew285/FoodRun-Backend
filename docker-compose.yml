services:
  db:
    image: postgres:16
    restart: always
    environment:
      POSTGRES_USER: foodrun
      POSTGRES_PASSWORD: foodrun123
      POSTGRES_DB: foodrun_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
volumes:
  postgres_data: